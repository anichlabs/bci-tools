stages:
  - verify

podman-test:
  stage: verify
  tags:
    - podman  # Only if you assigned this tag in your runner
  script:
    - echo "Podman version:"
    - podman --version
    - echo "Listing containers (should be empty):"
    - podman ps
    - echo "Podman is working inside GitLab CI"
